📚 Library Management System (Python + MySQL)
Overview

This project is a Library Management System developed using Python and MySQL. It automates library operations such as managing books, registering members, issuing and returning books, maintaining transaction history, and generating reports.

The project demonstrates database connectivity, CRUD operations, SQL JOINs, aggregate queries, and backend business logic using Python.
